let ededler = [12, 43, 74, 39, 55, 8];

for (let i = 0; i < ededler.length; i++) {
    if (ededler[i]%2 != 0) {
        // let ptag = document.createElement("p");
        // ptag.innerHTML = "cavab"
        console.log(ededler[i]);
        
    }
    
}